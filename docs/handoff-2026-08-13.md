# Handoff — estado a 13 de agosto de 2026

Documento de traspaso entre sesiones de Claude Code. **Lo permanente vive en `CLAUDE.md`**
(reglas, patrones, runbooks). Aquí solo está lo que estaba *a medias* el 13-ago-2026.
Cuando un punto se cierre, táchalo o bórralo.

## Estado del repo

- Rama de trabajo: `claude/fix-css-loading-RdK8j`, alineada con `main` en `b97b098`.
- Nada sin mergear. Último PR: **#96** (condiciones de muestras con IVA).
- Rama huérfana pendiente: `fix/cat-2905-hero-imagen` — un solo commit con la imagen del hero de
  platos de ducha baratos. **Cherry-pick o PR antes de borrarla** (regla de CLAUDE.md).

## Pendiente que depende de Ricardo (no lo puede hacer Claude)

### Categoría 5468 — Azulejos grandes para cocina
- **Meta description mal**: dice `60x120`, debe decir `40x120`. Esta página **no** vende 60x120;
  cualquier texto publicado que hable de ese formato aquí es falso.
- **Descripción del término vacía**: faltan los 5 H2 que la plantilla espera renderizar.
- Ofrecí generar el JSON de `mainEntity` (FAQ) a partir del HTML ya publicado — sin respuesta.

### Categoría 5467 — Azulejos grandes para baños
- Falta `og:image`.
- Verificar que `tono-blanco/` devuelve 200.

### Imágenes de hero pendientes (marcadas con `TODO` en el código)
5467, 5468, familia de muebles de dos senos, y platos de ducha baratos (2905).
Mientras tanto usan heros prestados de su propio silo.

### Espejos por medida (5472 · 120x80 y 5474 · 100x100)
Plantilla parametrizable `adrihosan/inc/category-espejo-medida.php` ya desplegada. Falta:
- **Pegar los cinco H2 en la descripción de cada término** (WooCommerce → editar categoría).
  La plantilla los imprime con `category_description()`; sin ellos ese bloque no sale.
- **Foto del hero**: ahora usa la genérica del silo de espejos. Hace falta un espejo de cada
  medida **montado en pared sobre el mueble** (marcado con `TODO` en el archivo).
- **Schema FAQ en Rank Math**, copiando palabra por palabra el HTML ya publicado.

### Servidor
- Activar en LiteSpeed **Cache → "Cache REST API"** (mitiga Complianz).
- Comprobar que `/wp-json/adrihosan/v1/` sigue en **"Do Not Cache URIs"**. Esto es config de
  WordPress, **no está en el repo**: si se migra de servidor o se resetean los ajustes, hay que
  volver a ponerlo o el sistema de reservas se rompe.

## Pendientes técnicos de fondo

Están detallados en la sección "AUDITORÍA 2026-07-17" de `CLAUDE.md` (puntos 1 a 7): HTML cruzado
en 4 categorías, paletas fuera de identidad, `mobile-fixes.css` con secciones de contenido,
`category-espejos.php` huérfano, mejoras del sistema de reservas y H1 fijos.

## Cómo arrancar una sesión nueva

1. `git fetch origin main && git rebase origin/main` (regla obligatoria de `CLAUDE.md`).
2. Leer `CLAUDE.md` entero: ahí están la identidad corporativa, el patrón de categorías, la
   política de muestras, el runbook de caídas y los errores que no hay que repetir.
3. Leer este handoff para saber qué quedó abierto.
4. Editar **solo** dentro de `adrihosan/`. El `inc/` de la raíz es una copia vieja.
